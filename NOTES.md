 in my claude.md I entered details about the project, how the project is structured and how to run the project

i did not enter anything to do with permissions, or anything specific to a directory

I explicity added three rules
- always allow tests
- never allow force push's
- always ask if i want to do a push

ive added these rules to ensure claude doesnt do something dangerous, for example a push force could cause the history to be overritten and break it for other people/developers

Running /memory confirms the CLAUDE.md is loaded, and running /permissions displays the configured allow/ask/deny rules."
